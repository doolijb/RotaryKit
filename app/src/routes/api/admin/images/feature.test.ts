import { fetch, apiRoute, basicUser, superUser, loginUser } from "$shared/testing"
import { test, expect } from "@playwright/test"
import api from "$shared/api"

test("admin emails GET: passes", async () => {
	/**
	 * Check the response
	 */
	expect(false).toBe(true)
})
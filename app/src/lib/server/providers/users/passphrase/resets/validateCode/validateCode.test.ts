import { test } from "vitest"

/**
 * Currently cannot unit test this file because vitest does not support
 * svelte-email rendering.
 * At the very least, this should already have coverage from the e2e tests.
 */

test("Send Passphrase Reset Code: Skipped", () => {
    // TODO: Implement rendering support in vitest
})
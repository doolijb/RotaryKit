import { expect, test } from "vitest"
import generateLocalToken from "."

test("generateLocalToken test passes", async () => {
    expect(false).toBe(true) // TODO
})

test("generateLocalToken test fails", async () => {
    expect(false).toBe(true) // TODO
})
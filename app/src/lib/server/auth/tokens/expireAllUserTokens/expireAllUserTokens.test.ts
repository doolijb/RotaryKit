import { expect, test } from "vitest"
import expireAllUserTokens from "."

test("expireAllUserTokens test passes", async () => {
    expect(false).toBe(true) // TODO
})

test("expireAllUserTokens test fails", async () => {
    expect(false).toBe(true) // TODO
})
import { expect, test } from "vitest"
import authenticateUser from "."

test("authenticateUser test passes", async () => {
    expect(false).toBe(true) // TODO
})

test("authenticateUser test fails", async () => {
    expect(false).toBe(true) // TODO
})
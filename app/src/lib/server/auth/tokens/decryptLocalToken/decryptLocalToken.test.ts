import { expect, test } from "vitest"
import decryptLocalToken from "."

test("decryptLocalToken test passes", async () => {
    expect(false).toBe(true) // TODO
})

test("decryptLocalToken test fails", async () => {
    expect(false).toBe(true) // TODO
})
import { expect, test } from "vitest"
import { Primitive } from "."

test("Primitive class has coverage", async () => {
	// Check that Primitive exists, this class is already tested in its children
    expect(Primitive).toBeDefined()
})
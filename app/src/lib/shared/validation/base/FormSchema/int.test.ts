import { expect, test } from "vitest"
import { FormSchema } from "."

test("FormSchema class has coverage", async () => {
	// Check that FormSchema exists, this class is already tested in its children
	expect(FormSchema).toBeDefined()
})
